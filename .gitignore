# Archive logs contain personal mail metadata (subjects, senders) - never commit.
ArchiveLog-*.csv
*.csv

# Transcripts from dry runs also capture subjects.
*.log

# Mail data
*.pst
*.ost
*.msg

# Windows / editor noise
Thumbs.db
desktop.ini
.vscode/
